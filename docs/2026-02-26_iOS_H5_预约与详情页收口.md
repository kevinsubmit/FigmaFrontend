# 2026-02-26 iOS/H5 预约与详情页收口

## 本次目标
- 对齐 iOS 端与 H5 端在预约链路、店铺详情、图片详情、Profile 的交互与视觉。
- 清理页面顶部冗余标题，保持移动端简洁层级。

## 主要完成内容

### 1) 预约链路（iOS）
- `Book services` 页面支持多服务选择与合计展示。
- 底部 `Continue` 进入底部弹层，补齐预约确认流程。
- 日历交互优化：过去日期禁用并灰显。
- 预约确认后增加成功过渡态，并跳转到 `Appointments`。
- `Group (Friends)` 预留客组流程与服务选择结构。

### 2) 店铺详情（iOS）
- `SERVICES / REVIEWS / PORTFOLIO / DETAILS` 页签与 H5 视觉对齐，字号下调与布局整理。
- `DETAILS` 区域地址卡片、背景图、按钮样式按 H5 风格收口。
- 作品流（Portfolio）与相关图片排列规则优化，减少错位。

### 3) 图片详情与跳转（iOS）
- `Choose a salon` 跳转至 `Book` 流程，并携带 style reference。
- 顶部按钮区安全区位置下移优化（刘海机型/非刘海机型兼容）。
- 文案与底部操作区层级向 H5 贴齐。

### 4) Profile 与二级页（iOS）
- Profile 指标卡、VIP 卡、设置入口、通知入口联动完善。
- 修复 Profile Settings 中 `Gender` / `Date of Birth` 重复显示问题。
- 补齐通知页、会员页、推荐页等跳转路径。

### 5) H5 同步
- 与预约相关的店铺服务/地址展示逻辑同步补齐。
- 页面头部冗余文案清理：
  - `appointments` 页面移除顶部 `APPOINTMENTS`
  - `deals` 页面移除顶部 `DEALS`

## 变更文件（核心）
- iOS:
  - `ios-app/NailsDashIOS/Sources/Features/Appointments/*`
  - `ios-app/NailsDashIOS/Sources/Features/Stores/*`
  - `ios-app/NailsDashIOS/Sources/Features/Home/HomeView.swift`
  - `ios-app/NailsDashIOS/Sources/App/*`
- H5:
  - `frontend/src/components/Deals.tsx`
  - `frontend/src/components/StoreDetails.tsx`
  - `frontend/src/services/stores.service.ts`
- Backend:
  - `backend/app/api/v1/api.py`
  - `backend/app/core/config.py`
  - `backend/app/services/risk_service.py`

## 说明
- 本次提交仅包含业务代码与文档。
- 本地运行缓存文件（如 `__pycache__`）、本地数据库与 `.env` 未纳入提交。
